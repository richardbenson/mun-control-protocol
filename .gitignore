# Visual Studio
bin/
obj/
.vs/
*.user
*.suo
*.lscache

# Rider
.idea/

# Per-machine MSBuild config (copy from Directory.Build.props.template)
Directory.Build.props.user

# Build outputs — KSP supplies these at runtime
deploy/GameData/KSPMissionControl/*.dll

# Release packaging output
publish/
KSPMissionControl-v*.zip

# Stub build output (source files are committed; binaries are not)
lib/stubs/**/bin/
lib/stubs/**/obj/
